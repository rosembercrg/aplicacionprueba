#compilar app angular
FROM node:22-alpine AS builder
WORKDIR /app
COPY Package*.json ./
RUN npm install
COPY . .
RUN npm run build --prod

#Servir

FROM nginx:alpine
COPY --from=builder /app/dist/aplicacion_prueba /usr/share/nginx/html
EXPOSE 80
CMD ["ngninx", "-g", "daemon off;"]
